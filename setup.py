from setuptools import setup, find_packages

setup(
    name="rkmcprotocol",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # 在这里列出您的依赖项，例如:
        # 'requests',
        # 'numpy',
    ],
)
